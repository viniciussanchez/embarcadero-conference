events {
}

http {
    resolver 127.0.0.11 valid=10s;

    server {
        listen 80;

        location / {
            set $upstream http://api:9000;
            proxy_pass $upstream;

            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
        }
    }
}
